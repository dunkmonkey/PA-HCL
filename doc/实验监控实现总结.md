# PA-HCL 实验监控功能实现总结

**日期**: 2026-01-24  
**版本**: v2.0  
**更新**: 移除 TensorBoard，统一使用 WandB

---

## 实现内容

PA-HCL 项目使用 **WandB (Weights & Biases)** 进行实验监控和可视化。

### 修改的文件

1. **`src/trainers/downstream_trainer.py`**
   - 添加 WandB 导入（可选依赖）
   - 在 `__init__` 方法中添加 3 个参数：
     - `use_wandb`: 启用 WandB
     - `wandb_project`: WandB 项目名称
     - `wandb_entity`: WandB 用户/团队名
   - 在训练过程中记录以下指标：
     - 训练指标（loss, accuracy, f1, precision, recall）
     - 验证指标（loss, accuracy, f1, auroc, auprc 等）
     - 测试指标（所有分类指标）
     - 学习率变化
   - 自动清理：训练结束后关闭 WandB

2. **`scripts/finetune.py`**
   - 添加命令行参数组 "实验监控"
   - 新增 3 个命令行参数：
     - `--wandb`: 启用 WandB
     - `--wandb-project`: WandB 项目名（默认 "PA-HCL"）
     - `--wandb-entity`: WandB 实体名（可选）
   - 将参数传递给 DownstreamTrainer

3. **`src/trainers/pretrain_trainer.py`**
   - 同样支持 WandB 监控
   - 记录预训练损失、学习率等指标

---

## 功能特性

### WandB

- ✅ 记录训练/验证/测试指标
- ✅ 记录学习率变化
- ✅ 记录系统资源（GPU、内存、CPU）
- ✅ 模型梯度和参数监控
- ✅ 完整的配置记录
- ✅ 云端存储，团队协作
- ✅ 自动生成报告
- ✅ 支持实验对比和超参数搜索

---

## 使用方法

### 基本用法

#### 1. 使用 WandB

```bash
# 首次使用需登录
wandb login

# 训练时启用
python scripts/finetune.py \
    --task circor_murmur \
    --pretrained checkpoints/pretrain/best_model.pt \
    --wandb \
    --wandb-project PA-HCL-Downstream
```

### 高级用法

#### 线性评估 + WandB

```bash
python scripts/finetune.py \
    --task circor_murmur \
    --pretrained checkpoints/pretrain/best_model.pt \
    --linear-eval \
    --wandb
```

#### 少样本学习 + WandB

```bash
python scripts/finetune.py \
    --task physionet2016 \
    --pretrained checkpoints/pretrain/best_model.pt \
    --few-shot \
    --shot-ratio 0.1 \
    --wandb \
    --wandb-project PA-HCL-FewShot
```

#### 批量实验

```bash
# 对所有任务运行实验并监控
for task in circor_murmur circor_outcome physionet2016 pascal
do
    python scripts/finetune.py \
        --task $task \
        --pretrained checkpoints/pretrain/best_model.pt \
        --wandb \
        --wandb-project PA-HCL-AllTasks
done
```

---

## 记录的指标

### 训练指标 (train/)

- `train_loss`: 训练损失
- `accuracy`: 准确率
- `f1`: F1 分数
- `precision`: 精确率
- `recall`: 召回率

### 验证指标 (val/)

- `val_loss`: 验证损失
- `val_accuracy`: 准确率
- `val_f1` / `val_f1_macro`: F1 分数
- `val_auroc`: ROC AUC
- `val_auprc`: PR AUC
- `val_precision`: 精确率
- `val_recall`: 召回率
- `val_sensitivity`: 敏感度
- `val_specificity`: 特异度

### 测试指标 (test/)

所有验证指标，使用 `test_` 前缀

### 学习率

- 编码器和分类器的学习率变化

### WandB 系统监控

- 系统指标（GPU 使用率、内存、CPU）
- 模型梯度分布
- 参数更新统计
- 完整配置信息

---

## 兼容性

### 可选依赖

WandB 为可选依赖：

- 如果未安装，会给出友好的警告信息
- 训练可以正常进行，只是不会记录监控数据
- 不会导致程序崩溃

### 安装

```bash
# 安装 WandB
pip install wandb

# 首次登录
wandb login
```

### 向后兼容

- 所有新参数都有默认值（默认禁用监控）
- 现有的训练脚本无需修改即可正常运行
- 只有显式指定参数才会启用监控

---

## 最佳实践

```bash
python test_tracking.py
```

检查项：
- TensorBoard 是否可用
- WandB 是否可用
- 训练器参数是否正确添加
- 微调脚本参数是否正确添加

### 运行示例脚本

```bash
bash scripts/demo_tracking.sh
```



### 使用建议

1. **所有正式实验**：使用 WandB 进行完整监控
2. **超参数搜索**：利用 WandB 的分组功能
3. **团队协作**：通过 wandb-entity 共享项目
4. **实验命名**：使用有意义的 experiment-name

---

## 目录结构

训练后的输出结构：

```
outputs/
└── circor_murmur_finetune/
    ├── wandb/                    # WandB 本地缓存
    │   └── run-xxx/
    ├── train.log                 # 训练日志
    ├── best_model.pt             # 最佳模型
    ├── epoch_10.pt               # 定期检查点
    └── final_metrics.json        # 最终指标
```

---

## 故障排除

### WandB 登录失败

```bash
wandb login --relogin
```

### 离线模式

```bash
export WANDB_MODE=offline
python scripts/finetune.py --task ... --wandb
```

### 禁用代码上传

```bash
export WANDB_DISABLE_CODE=true
```

---

## 示例场景

### 场景 1：超参数搜索

**需求**：测试不同学习率

**方案**：
```bash
for lr in 1e-3 5e-4 1e-4
do
    python scripts/finetune.py \
        --task circor_murmur \
        --pretrained checkpoints/pretrain/best_model.pt \
        --wandb \
        --wandb-project PA-HCL-HPSearch \
        --lr $lr \
        --experiment-name "lr_${lr}"
done
```

### 场景 2：团队协作

**需求**：团队成员共享实验结果

**方案**：
```bash
python scripts/finetune.py \
    --task circor_murmur \
    --pretrained checkpoints/pretrain/best_model.pt \
    --wandb \
    --wandb-project PA-HCL-Team \
    --wandb-entity team-name
```

### 场景 3：论文实验

**需求**：可复现的实验结果

**方案**：
```bash
python scripts/finetune.py \
    --task circor_murmur \
    --pretrained checkpoints/pretrain/best_model.pt \
    --wandb \
    --wandb-project PA-HCL-Paper \
    --seed 42
```

---

## 下一步

### 维护建议

1. 定期清理旧日志：`find outputs -type f -mtime +30 -delete`
2. 使用有意义的实验名称
3. 在 WandB 中使用标签组织实验
4. 定期导出重要实验结果

---

## 总结

PA-HCL 使用 WandB 进行实验监控：

✅ **易用性**：简单的命令行参数即可启用  
✅ **完整性**：记录所有重要指标和配置  
✅ **可靠性**：可选依赖，不影响核心功能  
✅ **兼容性**：向后兼容，无需修改现有代码  
✅ **协作性**：云端存储，团队共享

现在你可以：
- 实时监控训练过程
- 对比不同实验结果
- 与团队共享实验数据
- 生成可复现的实验记录

开始使用：
```bash
python scripts/finetune.py --task circor_murmur --pretrained ... --wandb
```

祝实验顺利！🚀
